{% extends '/admin/base_admin.html.twig' %}

{% block title %}Edit comment{% endblock %}

{% block body %}
<div class="container">
    <h1>Editing comment n°{{comment.id}}</h1>
    <div class="bg-primary p-3 rounded">
        <p><strong>Linked to ad : </strong>{{comment.ad.title}}</p>
        <p><strong>Author : </strong>{{comment.author.fullName}}</p>
        <p><strong>Posted at : </strong>{{comment.createdAt | date('d-m-Y')}}</p>
        <p><strong>Rating : </strong>{{comment.rating}}</p>
    </div>
    {{ form_start(form)}}
    {{ form_widget(form)}}
    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Save modifications</button>
    {{ form_end(form)}}
</div>

{% endblock %}